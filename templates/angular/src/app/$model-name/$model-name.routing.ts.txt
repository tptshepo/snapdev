import { Routes } from '@angular/router';
import { {{titlecase}}Component } from './{{dashlcase}}.component';
import { AuthGuard } from '../auth/auth-guard.service';

export const {{titlecase}}Routes: Routes = [
  {
    path: '',
    children: [
      {
        path: '',
        component: {{titlecase}}Component,
        canActivate: [AuthGuard],
        data: {
          title: '{{ptitlecase}}',
          urls: [{ title: 'Home' }, { title: '{{ptitlecase}}' }]
        }
      }
    ]
  }
];
