<!-- content row -->
<div class="row">

  <!-- NEW WIDGET START -->
  <article class="col-sm-12 col-md-12 col-lg-12 col-center-block">
    <div class="spinner-panel" style="right: 30px;top: 7px;" *ngIf="(serverCallsInProgress | async) > 0">
      <app-spinner [size]="40" [tickness]="2" [color]="'#ff1d1d'">
      </app-spinner>
    </div>
    <!-- Widget ID (each widget will need unique ID)-->
    <div class="jarviswidget jarviswidget-sortable" data-widget-colorbutton="false"
      data-widget-editbutton="false" role="widget">

      <!-- widget div-->
      <div role="content">

        <!-- widget content -->
        <div class="widget-body">

          <form #f="ngForm" class="form-horizontal" (ngSubmit)="saveForm()" novalidate>

            <fieldset>
              <legend>Create {{title}}</legend>

              <!-- Show error list -->
              <div class="alert alert-block alert-info" *ngIf="errors?.length > 0">
                <ul>
                  <li *ngFor="let e of errors">{{=<% %>=}}{{<%={{ }}=%>e{{=<% %>=}}}}<%={{ }}=%></li>
                </ul>
              </div>

              <article class="col-sm-12 col-md-12 col-lg-6">

                {{#properties}}
                {{#formcreate}}
                {{#foreignobject}}
                <div class="form-group">
                  <label for="{{titlecase}}" class="control-label col-md-3">{{title}}</label>
                  <div class="col-md-9">
                    <ng2-completer [ngModelOptions]="{standalone: true}" [(ngModel)]="{{camelcase}}SearchString" (selected)="{{camelcase}}CompleterSelected($event)" (blur)="{{camelcase}}CompleterBlur()" [datasource]="{{camelcase}}DataService" [minSearchLength]="1" inputClass="form-control" placeholder="{{placeholder}}" selectOnClick="true"></ng2-completer>
                    {{#required}}
                    <small class="help-block field-error" *ngIf="!item.{{camelcase}}Id">Field is required</small>
                    {{/required}}
                  </div>
                </div>
                {{/foreignobject}}
                {{#textinput}}
                <div class="form-group">
                  <label for="{{titlecase}}" class="control-label col-md-3">{{title}}</label>
                  <div class="col-md-9">
                    <input class="form-control" id="{{titlecase}}" name="{{titlecase}}" type="text" [(ngModel)]="item.{{camelcase}}" required
                      #{{camelcase}}="ngModel">
                    {{#required}}
                    <small class="help-block field-error" *ngIf="!{{camelcase}}.valid">Field is required</small>
                    {{/required}}
                  </div>
                </div>
                {{/textinput}}
                {{/formcreate}}
                {{/properties}}

                
              </article>

            </fieldset>

            <!-- form action start -->
            <div class="form-actions">
              <div class="row">
                <div class="col-md-12">
                  <button class="btn btn-primary" type="submit"><i class="fa fa-save"></i> Save</button> &nbsp;
                  <a [routerLink]="['/{{plcase}}']" class="btn btn-default pull-right"><i class="fa fa-times-circle-o"></i>
                    Cancel</a>
                </div>
              </div>
            </div>
            <!-- form action end -->

          </form>

        </div>
        <!-- end widget content -->

      </div>
      <!-- end widget div -->

    </div>
    <!-- end widget -->

  </article>
  <!-- WIDGET END -->

</div>
<!-- end content row -->