using System;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.ComponentModel.DataAnnotations;
using System.ComponentModel.DataAnnotations.Schema;

namespace {{package}}.Models
{
  [Table("{{ptitlecase}}")]
  public class {{titlecase}} : BaseModel
  {
    {{#properties}}
    {{#notinbaseclass}}
    {{#required}}[Required]{{/required}}{{#stringlength}}[StringLength({{.}})]{{/stringlength}}
    public {{{type}}} {{titlecase}} { get; set; }
    {{/notinbaseclass}}
    {{/properties}}
  
    public {{titlecase}}()
    {
      {{#properties}}
      {{#notinbaseclass}}
      {{#constructorinit}}
      {{titlecase}} = new {{{typenew}}};
      {{/constructorinit}}
      {{/notinbaseclass}}
      {{/properties}}
    }
  }
}
